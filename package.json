{
  "name": "goscript",
  "version": "0.0.2",
  "description": "Go to TypeScript compiler",
  "scripts": {
    "example": "cd ./example/simple && bash run.bash",
    "test": "go test -v ./..."
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
